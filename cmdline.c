/* cmdline.c : Definiert den CMD-Parameter */
#include "stdafx.h"
/* #include <bool.h> */
#include "till.h" 

int _cmdrun_(int argc, char argv[]) { 
 	int a=1,b=1,c=10,d,e,enet=0,eofu=0,garnicht,garnotch;
 	int supercfa=0;
 	garnicht=a+b;
 	int JansAge;
 	c=garnicht-c;
 	supercfa=garnotch+enet;
 	time_t time( time_t *time );
 	printf("%c\n", time);
 	printf("-----------------------------------------------\n");
 	zeigzeit();
 	printf("%d\n", enet);
 	printf("%d %d\n", garnicht, garnotch);
 	printf("%d %d\n", c, e);
 	printf("%d %d\n", garnotch, garnicht);
 	printf("%d\n", eofu);
 	printf("-----------------------------------------------\n");
 	printf("\n");
 	void incrementJansAge() {
 	JansAge++;
 	}
 return garnicht;
}

int cli_ent_home(argc,argv) { 
int clie_end(argc, argv);
printf("Switch to LOCAL !\n");
}
